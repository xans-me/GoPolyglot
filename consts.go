package GoPolyglot

// ContentTypeJSON represents the MIME type for JSON content.
const ContentTypeJSON = "application/json"

// AcceptedLanguageHeader is the HTTP header key used to specify the desired language for translations.
const AcceptedLanguageHeader = "X-Accept-Language"
